#include<bits/stdc++.h>
using namespace std;
int main(){
    string name;
    cin>>name;
    char a;
    cin>>a;
    int count=0;
    for(int i=0;i<name.length();i++){
        if(name[i]==a){
            // count++;
            cout<<name[i];
        }
    };
    
// cout<<"The letter appears that times "<<count;
}